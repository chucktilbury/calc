#include <stdio.h>
#include <stdbool.h>

#include "value.h"

Value* createValue(ValueType type, Token* tok) {

    (void)type;
    (void)tok;

    return NULL;
}

void destroyValue(Value* val) {

    (void)val;
}

